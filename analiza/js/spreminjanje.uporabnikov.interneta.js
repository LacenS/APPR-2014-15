(function($) {
    $(document).ready(function() {
	
	$('#spreminjanje_uporabnikov_interneta').scianimator({
	    'images': ['images/spreminjanje.uporabnikov.interneta1.pdf'],
	    'width': 900,
	    'delay': 1000,
	    'loopMode': 'loop'
	});
    });
})(jQuery);
